# Cloudflare Pages 配置文件
# https://developers.cloudflare.com/pages/configuration/

[build]
  command = "bun run build"
  publish = "dist"

[[env.production]]
  [build]
    command = "bun run build"
    publish = "dist"

[[env.preview]]
  [build]
    command = "bun run build"
    publish = "dist"
